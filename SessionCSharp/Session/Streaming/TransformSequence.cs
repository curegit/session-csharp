using System;
using System.IO;
using System.Collections.Generic;

namespace Session.Streaming
{
	/*
	public sealed class TransformSequence : ITransform
	{
		//private readonly ITransform top;

		public TransformSequence(ITransform[] transforms)
		{
			if (transforms is null) throw new ArgumentNullException(nameof(transforms));
			top = this;
			foreach (var transform in transforms)
			{
				
			}
		}

		public Stream DecorateIncomingStream(Stream stream)
		{
			return readableStream;
		}

		public Stream DecorateOutgoingStream(Stream stream)
		{
			return writableStream;
		}
	}
	*/
}
